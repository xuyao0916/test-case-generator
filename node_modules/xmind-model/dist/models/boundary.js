"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const constants_1 = require("../common/constants");
const range_component_1 = require("./range-component");
const GET = 'get';
const SET = 'set';
const TITLE = 'title';
class Boundary extends range_component_1.default {
    constructor() {
        super(...arguments);
        this.componentType = constants_1.COMPONENT_TYPE.BOUNDARY;
    }
    getTitle() {
        return this._data.commit(GET, TITLE);
    }
    changeTitle(title) {
        const oldTitle = this.getTitle();
        if (oldTitle === title) {
            return;
        }
        this._data.commit(SET, TITLE, title);
        this.getUndo().add({
            undo: () => { this.changeTitle(oldTitle); },
            redo: () => { this.changeTitle(title); }
        });
    }
}
exports.default = Boundary;
//# sourceMappingURL=boundary.js.map